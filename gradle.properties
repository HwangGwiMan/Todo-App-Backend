# Gradle 데몬 메모리 설정 (대폭 증가)
org.gradle.jvmargs=-Xmx8g -Xms2g -XX:MaxMetaspaceSize=1g -XX:+HeapDumpOnOutOfMemoryError

# Gradle 데몬 사용
org.gradle.daemon=true

# 병렬 빌드 활성화
org.gradle.parallel=true

# 빌드 캐시 활성화
org.gradle.caching=true

# Configuration on demand (선택적)
org.gradle.configureondemand=false
